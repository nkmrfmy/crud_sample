<p>新規投稿</p>
<!-- form_with(ヘルパーメソッド) 紐づけられたモデルオブジェクト(@user)がテーブルに存在するか判定する 
存在してなければcreate、存在すればupdateを自動的にルーティングを指定する -->
<%= form_with model: @user, local: true do |f|%>
  <p>お名前<%= f.text_field :name %></p>
  <p>年齢<%= f.text_field :age %></p>
  <%= f.submit "送信"%>
<% end %>
